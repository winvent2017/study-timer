# 작업 지시: 몰입 도입 연출 전면 교체 — 세트 사각형 확장 방식

> 프로젝트: EasyDeeper (study-timer) / 작업일 2026-09-13
> 이 작업은 기존 도입 연출(2초 암전 → 3,2,1,0 카운트 → 문구 → pause ‖ 기호)을 **완전히 대체**한다.
> `VISUAL_ONLY_MODE = true`는 그대로 유지한다 (알람/확률/축하/휴식/요약 흐름은 여전히 꺼둔 상태).

---

## 0. 배경과 목표

현재 설정 화면(크림/오렌지 톤)에서 시작하기를 누르면 별도의 어두운 화면으로 갈아끼워지는데, 두 화면 사이에 시각적 연결 고리가 없어 디자인 통일성이 깨진다. 이를 해결하기 위해 **설정 화면 자체가 몰입 화면으로 변형**되는 방식으로 바꾼다.

핵심 그림:

1. 설정 화면의 세트 그리드에서 **이번에 시작할 세트 순번의 사각형**이 자기 색을 유지한 채 **페이지 전체로 확장**되어 나머지 UI를 덮는다.
2. 확장 중에도, 그리고 확장 후에도 **EasyDeeper 로고(상단)**와 **시작하기 버튼(하단)**만 사각형 위에 떠 있다. 로그인/회원가입 버튼을 포함한 다른 모든 요소는 덮여서 보이지 않는다.
3. 확장 완료 후 시작하기 버튼 라벨이 **중단하기**로 바뀐다.
4. 확장된 밝은 사각형 위에서 세트 순번 → 3, 2, 1 카운트 → 몰입 유도 문구가 순서대로 표시된다.
5. 마지막 문구("이 화면은 서서히 어두워질 거예요")가 표시된 0.5초 뒤 살짝 어두워지고, 이후 **45초에 걸쳐** 서서히 최종 어둠(`--immersion-bg: #201710`)까지 진행된다. 로고와 버튼은 끝까지 남는다.

---

## 1. 용어 (코드 주석·변수명에 반영)

- **세트(set)**: 학습 + 휴식 한 묶음. 그리드의 사각형 하나 = 1세트.
- **세션(session)**: 유저가 목표로 정한 세트 여러 개의 전체 묶음.
- 이번 작업에서 "현재 세트 순번"은 세션 내에서 지금 시작하는 세트의 1-based 번호를 뜻한다 (예: 5세트 세션에서 1세트 완료 후 시작하면 2).

---

## 2. 삭제할 것 (죽은 코드 원칙 적용)

다음은 **완전히 삭제**한다. "나중에 쓸지도 모른다"며 남기지 않는다. git 이력으로 복원 가능하다.

- pause(‖) 기호 컴포넌트 및 관련 CSS (둥근 세로 막대 2개, opacity 0.35 hover 처리 등)
- pause ‖ → 사각형 대화 프레임 변형 연출 전체 (회전 220ms, SVG path stroke-dashoffset 선 그리기, 콘텐츠 페이드인, 프레임 크기/앵커 계산, 테두리색 `#8a7f72` 관련 스타일)
- 대화 프레임 안의 질문/버튼 UI ("잠시 멈춰볼까요?", "조금만 더 해볼래요", "여기까지 할게요")
- `src/lib/strings/ko.ts`의 `pauseDialog` 섹션
- 프레임이 열린 동안 경과 시간을 일시정지/재개하던 로직 — **프레임 전용이었다면 삭제**. 다른 곳에서도 쓰이는 범용 함수라면 남기되 이번 작업 범위에서 호출부는 제거
- 기존 도입 시퀀스 데이터(`immersionSequences.ts`의 `DEFAULT_SEQUENCE`) 중 이번 스펙에 없는 step: 선행 2.0s 어두워짐, 0.5s 정적(어두워진 뒤의), 숫자 0, 기존 문구 등. 새 시퀀스(4절)로 **교체**한다.
- 위 항목만 사용하던 타입, 유틸 함수, 상수, 테스트, 주석

**건드리지 않는 것**: `VISUAL_ONLY_MODE` 플래그로 꺼둔 기존 StudyScreen / ImmersionMode / AlarmModal / CelebrationModal / BreakScreen / SummaryScreen 등은 수정·삭제하지 않는다.

---

## 3. 확장 연출 스펙

### 3.1 시작 조건

- 설정 화면에서 "시작하기" 클릭.
- 현재 세트 순번 `n`에 해당하는 그리드 사각형(1-based, 좌상단부터 행 우선)을 **원본(source)**으로 잡는다.

### 3.2 확장 애니메이션 (FLIP 방식 권장)

- 원본 사각형의 화면 좌표(`getBoundingClientRect`)를 측정한 뒤, 동일한 위치·크기·색·border-radius의 **오버레이 요소**를 `position: fixed`로 생성한다.
- 오버레이를 **뷰포트 전체**(`inset: 0`, border-radius 0)로 확장한다.
  - 시간: **800ms**, easing: `cubic-bezier(0.4, 0, 0.2, 1)` 또는 동등한 ease-in-out
  - 색: 그리드 사각형과 **동일한 연한 오렌지 토큰**을 그대로 사용 (새 색 정의 금지)
- 원본 사각형은 확장 시작과 동시에 시각적으로 오버레이 뒤에 가려지므로 별도 처리 불필요. 다만 그리드/설정 UI는 DOM에 남겨도 되고 unmount해도 되는데, **로고와 버튼을 제외한 모든 것은 오버레이보다 낮은 z-index**로 확실히 덮여야 한다.
- 로고(헤더 좌측)와 시작하기 버튼은 **오버레이보다 높은 z-index**로 제자리에 고정되어, 확장 동안 위치·크기가 변하지 않는다.
- 로그인/회원가입 버튼은 오버레이에 덮여 보이지 않는다. (헤더 컴포넌트에 "로고만 표시" 모드를 추가하거나, 우측 버튼 영역만 낮은 z-index로 두는 방식 중 단순한 쪽 선택)

### 3.3 확장 완료 시

- 시작하기 버튼 라벨을 **중단하기**로 교체. 라벨 교체는 텍스트 크로스페이드 **200ms**. 버튼의 크기·위치·색은 유지.
- 확장 완료 직후부터 4절의 텍스트 시퀀스 시작.

### 3.4 중단하기 버튼 임시 동작

- 클릭 시 **설정 화면으로 복귀** (오버레이 제거, 어두워짐 상태 초기화). 복귀 애니메이션은 넣지 않는다 — 즉시 전환.
- 핸들러는 별도 함수로 분리하고 다음 주석을 유지:
  `// TODO: 중단하기 클릭 후 반응(최소 목표시간 달성/미달성 분기 등)은 별도 설계 예정 — 현재는 임시로 설정 화면 복귀`

---

## 4. 텍스트 시퀀스 스펙

### 4.1 데이터 구조

- `src/lib/immersionSequences.ts`의 시퀀스 객체를 이번 스펙에 맞게 **교체**한다. 컴포넌트에 숫자·문구·시간을 하드코딩하지 않는다.
- step 타입은 최소 다음을 표현할 수 있어야 한다:
  - `pause` (정적, durationMs)
  - `setLabel` (현재 세트 순번 표시, durationMs — 실제 문자열은 strings의 함수 템플릿으로 생성)
  - `count` (숫자, durationMs)
  - `text` (strings 키 참조, durationMs)
  - `dimStart` (어두워짐 시작 트리거 — 이전 text step 시작 후 delayMs, 이후 초기 살짝 어두워짐 + 긴 어두워짐 파라미터)
- 기존 `audioSrc` 필드는 타입에 그대로 유지한다 (재생 로직은 여전히 미구현).

### 4.2 시퀀스 내용 (기본 시퀀스)

| 순서 | step          | 내용                                                      | 시간       |
| ---- | ------------- | --------------------------------------------------------- | ---------- |
| 0    | pause         | 정적                                                      | 500ms      |
| 1    | setLabel      | "{n}세트" (작은 글씨)                                     | 1000ms     |
| 2    | count         | 3                                                         | 1000ms     |
| 3    | count         | 2                                                         | 1000ms     |
| 4    | count         | 1                                                         | 1000ms     |
| 5    | text          | 자, 이제 시작합니다.                                      | 2000ms     |
| 6    | text          | 당신은 천천히 몰입의 세계로 들어갑니다.                   | 3000ms     |
| 7    | text          | 이 화면은 서서히 어두워질 거예요.                         | 3000ms     |
| —    | dimStart      | 7번 text가 **표시된 시점으로부터 500ms 후** 어두워짐 시작 | (아래 5절) |
| 8    | (텍스트 없음) | 어두워짐만 계속                                           | —          |

- **0은 카운트하지 않는다** (3 → 2 → 1로 끝).
- 각 텍스트/숫자는 화면 중앙, 한 번에 하나만, fade in/out (fade 각 200~250ms, durationMs 안에 포함).
- 텍스트 색: **진한 웜 브라운**(어두운 배경색 `#201710` 또는 그에 가까운 기존 토큰). 밝은 오렌지 배경 위에서 충분한 대비 확보. 7번 문구가 사라질 때까지 배경은 45초 어두워짐의 초반(약 2.5초 경과)이라 여전히 밝으므로 텍스트 색 전환은 불필요하다.
- setLabel은 count/text보다 **작은 크기**로, 카운트 숫자는 가장 크게, 문구는 중간 크기. 기존 도입 연출의 폰트 크기 체계를 참고해 유지.
- 경과 시간(내부 측정, 화면 미표시)은 **5번 step("자, 이제 시작합니다") 표시 시작 시점**부터 측정한다. 이 시작 시점은 상수/설정으로 분리해 나중에 바꿀 수 있게 한다.

### 4.3 문자열

- 모든 화면 텍스트는 `src/lib/strings/ko.ts`에 둔다. JSX 하드코딩 금지 (`CLAUDE.md`, `AGENTS.md` 컨벤션).
- 신규 섹션 예시 (키 이름은 기존 네이밍 관례에 맞춰 조정 가능):
  ```ts
  immersionIntro: {
    setLabel: (n: number) => `${n}세트`,
    start: "자, 이제 시작합니다.",
    enter: "당신은 천천히 몰입의 세계로 들어갑니다.",
    dimNotice: "이 화면은 서서히 어두워질 거예요.",
  },
  session: {
    stopButton: "중단하기",
  }
  ```
- 카운트 숫자(3, 2, 1)는 문자열이 아닌 숫자 데이터로 두고 렌더 시 문자열화한다 (로케일별 숫자 표기 대응 여지).
- 기존 "시작하기" 버튼 문자열은 기존 키를 그대로 사용한다.

---

## 5. 어두워짐 스펙

- 진행도 `p` (0 → 1)를 단일 값으로 관리하고, 배경·로고·버튼 색을 모두 이 `p`에서 파생시킨다 (CSS 커스텀 프로퍼티 하나를 갱신하는 방식 권장). 어두워짐 관련 시간값은 시퀀스 데이터에 둔다.
- **1단계 — 초기 살짝 어두워짐**: 7번 문구 표시 시작 + 500ms 시점에 `p`를 0에서 **0.08**까지 **400ms**로 이동. 사용자가 "시작됐다"고 인지할 만큼의 미세한 변화.
- **2단계 — 긴 어두워짐**: 1단계 종료 직후부터 `p`를 0.08 → 1.0까지 **45,000ms**에 걸쳐 진행. easing은 `linear` 또는 아주 완만한 ease-out. 순간순간 변화가 의식되지 않아야 한다.
- 배경: 연한 오렌지(확장된 사각형 색) → `#201710`을 `p`로 보간.
- 로고: 현재 진한 글자색 → 웜 그레이(기존 `#8a7f72` 계열 토큰 재사용 가능), opacity 1 → **0.5**를 `p`로 보간.
- 중단하기 버튼: 오렌지 유지하되 `p`가 커질수록 채도·밝기를 소폭 낮춰 어두운 배경에서 튀지 않게 (최종 상태에서 여전히 명확히 버튼으로 인식 가능해야 함). 텍스트 대비는 유지.
- 최종 상태(`p = 1`): 어두운 화면 + 은은한 로고 + 중단하기 버튼. 시간 정보는 어디에도 표시하지 않는다.

---

## 6. 접근성

- `prefers-reduced-motion: reduce`일 때:
  - 확장 애니메이션 생략 — 오버레이를 즉시 전체 화면으로 표시
  - 텍스트 시퀀스는 동일한 순서·시간으로 표시하되 fade 없이 즉시 표시/제거
  - 어두워짐은 7번 문구 종료 시점에 `p = 1`로 즉시 적용

---

## 7. 구현 시 유의사항

- 좌표·크기는 뷰포트 비율(%)이 아니라 실제 측정값(FLIP)과 `inset: 0`을 사용한다. 데스크톱·모바일 모두에서 사각형이 정확히 뷰포트를 채워야 한다.
- 오버레이가 스크롤 영역과 무관하게 뷰포트를 덮도록 `position: fixed` 사용. 확장 후 페이지 스크롤은 잠근다.
- 확장 중/후에 창 크기가 바뀌어도 오버레이는 `inset: 0`을 유지해야 한다.
- 로고와 버튼의 z-index는 오버레이보다 확실히 높게, 나머지 모든 설정 UI는 낮게.
- 새 컴포넌트/훅을 만들 경우 기존 디렉터리 구조(`components/`, `hooks/`, `lib/`)를 따른다.

---

## 8. 완료 후 확인 사항 (체크리스트)

- [ ] 설정 화면에서 시작하기 클릭 시, **현재 세트 순번**의 사각형이 확장된다 (1세트 시작 → 1번째, 임시로 순번 값을 바꿔 2번째·5번째도 확인)
- [ ] 확장 중·후에 로고와 버튼만 보이고, 제목·부제·슬라이더·다른 사각형·안내문·로그인/회원가입 버튼은 모두 덮인다
- [ ] 확장 완료 시 버튼 라벨이 "중단하기"로 바뀌고, 위치·크기는 그대로다
- [ ] "{n}세트" → 3 → 2 → 1 → 문구 3개가 순서·시간대로 표시되며, 0은 나오지 않는다
- [ ] 텍스트가 밝은 배경 위에서 선명하게 보인다 (이전 "밝은 배경에 밝은 숫자" 문제 재발 없음)
- [ ] 마지막 문구 표시 후 약 0.5초에 살짝 어두워지고, 이후 45초에 걸쳐 서서히 완전히 어두워진다
- [ ] 어두워짐에 맞춰 로고·버튼 색도 함께 변하며, 최종 상태에서 버튼이 여전히 명확히 보인다
- [ ] 화면 어디에도 경과 시간·남은 시간이 표시되지 않는다
- [ ] 중단하기 클릭 시 설정 화면으로 복귀하고, 다시 시작하기를 누르면 처음부터 정상 동작한다
- [ ] 데스크톱(가로 넓은 창)과 모바일 폭(≈360px) 양쪽에서 오버레이가 뷰포트를 정확히 채운다
- [ ] `prefers-reduced-motion` 환경에서 6절대로 동작한다
- [ ] pause ‖ 기호, 대화 프레임 연출, `pauseDialog` 문자열, 관련 CSS·타입·유틸이 **어디에도 남아 있지 않다** (grep으로 확인)
- [ ] `src/lib/strings/ko.ts` 외에 JSX/TS에 화면 텍스트 하드코딩이 없다 (grep으로 한글 리터럴 확인)
- [ ] `VISUAL_ONLY_MODE = true`가 유지되며, 꺼둔 기존 컴포넌트는 변경되지 않았다
- [ ] 빌드·린트 에러 없음
