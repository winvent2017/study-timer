# 프롬프트: 몰입 도입 연출 타이밍 수정 (어두워진 후 카운트 시작)

## 목적

현재 도입 연출은 배경이 아직 밝은 크림색인 상태에서 웜 화이트 숫자가 표시되어, 초반 숫자(5, 4)가 대비 부족으로 거의 보이지 않는 문제가 있다. 연출 순서를 "먼저 완전히 어두워진 뒤, 그 어둠 위에서 카운트 시작"으로 재구성한다. 카운트는 3, 2, 1, 0으로 변경한다.

## 변경 사항

### 1. 새 연출 타임라인

시작하기 클릭 시점부터 다음 순서로 진행한다:

1. **어두워짐 (2.0s)**: 설정 화면이 opacity 0으로 사라지고, 배경이 `--background`(크림)에서 `--immersion-bg`(#201710)로 전환. 2초 후 화면은 완전한 어둠 상태에 도달. **이 단계가 끝나기 전에는 어떤 숫자·문구도 표시하지 않는다**
2. **정적 (0.5s)**: 아무것도 없는 어두운 화면 유지 (전환감을 위한 의도적 공백)
3. **카운트 (숫자당 1.2s × 4 = 4.8s)**: "3" → "2" → "1" → "0" 순서로 화면 중앙에 큰 숫자 표시. 각 숫자는 durationMs 안에서 fade-in → 머묾 → fade-out. 스타일(크기, `--immersion-fg` 색상, 얇은 굵기)은 기존 그대로
4. **몰입 유도 문구 (3.5s)**: "0"이 사라짐과 동시에 기존 문구("자, 이제 당신은 천천히 몰입의 세계에 빠지게 됩니다") fade-in → 머묾 → fade-out 시작
5. **pause 등장 (약 1.0s)**: 문구가 사라지는 것과 교차(crossfade)하며 pause 표시가 서서히 나타남 (최종 opacity 0.35)
6. **완성 상태**: 어두운 화면 + 은은한 pause 표시만 존재 (기존과 동일)

### 2. 시퀀스 데이터 구조 반영

- 위 타임라인을 컴포넌트 하드코딩이 아닌 `immersionSequences.ts`의 `DEFAULT_SEQUENCE` 데이터로 표현한다
- 필요 시 `ImmersionStepType`에 단계 타입을 추가한다 (예: `'darken'`, `'silence'`). 타입 확장 시 `src/types/index.ts`와 재생 컴포넌트의 step 처리 로직을 함께 갱신
- 어두워짐 2.0s, 정적 0.5s도 시퀀스 데이터의 durationMs로 정의해 추후 커스텀 시퀀스에서 조정 가능하게 한다
- 기존 "5, 4" 등 이번 타임라인에 없는 step 데이터는 DEFAULT_SEQUENCE에서 제거 (이번 작업에서 새로 불필요해지는 데이터이므로 남기지 말 것)

### 3. 유지할 것

- pause 표시의 모양·위치·인터랙션(은은함 → hover/탭 시 선명), 클릭 시 setup 복귀 동작은 변경 없음
- `VISUAL_ONLY_MODE` 플래그와 기존 흐름 비활성화 상태 유지
- 경과 시간 내부 측정 유지 (화면 표시 금지)
- 텍스트 중앙화(strings/ko.ts) 컨벤션 유지 — 문구 변경 없음

### 4. 모션 접근성

- `prefers-reduced-motion: reduce`: 배경은 트랜지션 없이 즉시 어두운 상태로 전환하고, 숫자·문구는 fade 없이 순차 표시. 순서와 지속 시간은 동일하게 유지

## 구현 시 참고

- 핵심은 순서 보장이다: 배경 전환 완료(2.0s) 이전에 카운트 step이 시작되지 않도록 시퀀스 진행 로직에서 단계를 직렬로 처리할 것. CSS transition 지속시간과 step durationMs가 어긋나 숫자가 미리 튀어나오는 일이 없어야 한다
- 문구 fade-out과 pause fade-in의 교차는 자연스러움 우선으로 미세 조정 가능 (완전 순차여도 무방하나 겹치는 쪽이 부드러움)
- 이 수정으로 전체 도입 길이는 약 12초가 된다 (2.0 + 0.5 + 4.8 + 3.5 + 1.0)

## 완료 후 확인 사항

- [ ] 시작하기 클릭 후 2초 동안 숫자·문구 없이 어두워지기만 하는지
- [ ] 완전히 어두워진 뒤 0.5초의 정적이 있는지
- [ ] 카운트가 3, 2, 1, 0 순서이고 (5, 4 없음) 각 숫자가 어두운 배경 위에서 또렷하게 보이는지
- [ ] 0이 사라짐과 동시에 몰입 유도 문구가 나타나는지
- [ ] 문구가 사라지며 pause 표시가 교차로 서서히 나타나는지
- [ ] 최종 상태(어두운 화면 + 은은한 pause)와 pause 클릭 → setup 복귀가 기존대로 동작하는지
- [ ] 타임라인 수치가 immersionSequences.ts 데이터로 정의되어 있고, DEFAULT_SEQUENCE에 5·4 등 불필요한 step이 남아 있지 않은지
- [ ] prefers-reduced-motion 환경에서 애니메이션 없이 동일한 순서로 표시되는지
- [ ] VISUAL_ONLY_MODE, 경과 시간 측정, 텍스트 중앙화가 그대로인지
