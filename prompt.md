# 프롬프트: 세트 설정 UI 개선 (순서 변경 + 시각화 그리드 추가)

## 목적

설정 화면(SetupScreen)에서 "세트 반복 횟수"를 사용자가 직관적으로 체감할 수 있도록 정사각형 그리드 프리뷰를 추가하고, 항목 순서와 라벨을 조정한다.

## 변경 사항

### 1. 라벨 텍스트 변경

- `src/lib/strings/ko.ts`에서 "최소 학습시간" 라벨을 **"1세트 최소 학습시간"**으로 변경
- setup 화면 관련 키를 찾아 값만 수정, 키 이름 구조는 유지

### 2. 항목 순서 변경

- 기존: 최소 학습시간 → 휴식시간 → 세트 반복 횟수 → 확률
- 변경: **1세트 최소 학습시간 → 세트 반복 횟수 → 휴식시간 → 확률**
- `SetupScreen.tsx` 내 JSX 블록의 순서만 이동. 각 항목의 상태(state)/로직은 변경하지 않음

### 3. 세트 반복 횟수: 입력 방식을 드래그 슬라이더로 전환 + 정사각형 그리드 시각화

**3-1. 입력 방식 변경**

- 기존 숫자 입력창(스피너)을 삭제하고, "1세트 최소 학습시간"과 동일한 스타일의 **드래그 슬라이더**로 교체
- 슬라이더 우측 상단에 실시간 숫자 라벨 표시 (예: "4회") — 1세트 최소 학습시간 슬라이더와 동일한 레이아웃 패턴
- **무제한 체크박스는 완전히 삭제**
- 슬라이더 범위: **최소 1회 ~ 최대 20회**
- 슬라이더 간격: **균등(linear)**. 구간별 민감도 차이 없음

**3-2. 그리드 갱신 타이밍 + 애니메이션**

- 드래그하는 동안에는 그리드를 갱신하지 않음 (숫자 라벨만 실시간 갱신)
- **마우스/터치를 뗀 순간(release)에만** 그리드가 새 개수로 갱신
- 갱신 시 사각형이 순간적으로 나타나거나 사라지지 않고, **늘어나거나 줄어드는 자연스러운 트랜지션 애니메이션** 적용 (예: scale/opacity 트랜지션, 새로 추가되는 사각형은 살짝 커지며 나타나고 제거되는 사각형은 줄어들며 사라짐. 200~300ms 권장)

**3-3. 그리드 배열 규칙**

- **열 개수는 항상 5개로 고정** (더 이상 √n 기반 가변 열 아님)
- **CSS Grid 사용, `grid-template-columns: repeat(5, 1fr)`로 컨테이너 너비를 정확히 5등분** — 고정 px 크기가 아니라 비율(1fr) 기반으로 셀 너비를 정해서, 셀들이 좌우로 늘어나며 컨테이너 우측까지 여백 없이 꽉 채워지도록 함 (그 결과 정사각형이 아니라 가로로 살짝 넓은 직사각형 형태가 됨 — 의도된 결과)
- 예: 3회 → 1줄에 3개 (왼쪽부터 배치, 나머지 2칸은 빈 공간)
- 예: 16회 → 5개씩 3줄(15개) + 4번째 줄 왼쪽에 1개
- 마지막 줄이 다 채워지지 않을 경우, CSS Grid 특성상 자연스럽게 **왼쪽부터 채워지고 오른쪽은 빈 칸**으로 남음 (별도 정렬 로직 불필요)
- **사각형 내부에는 텍스트를 넣지 않음** (예: "5분" 반복 표시 금지) — 순수한 색상 도형으로만 표시해 "덩어리감"만 시각적으로 전달

**3-4. 사각형(직사각형) 크기 및 간격**

- 너비는 3-3의 규칙대로 컨테이너를 5등분한 비율(1fr)로 자동 결정 (고정 px 지정하지 않음)
- 높이는 참고 이미지 기준 "작은 사각형 2×2(4개)가 뭉쳐 있는 면적"과 비슷한 시각적 크기감을 주는 값으로 고정 (예: 56~64px 정도, 실제 값은 구현 시 비율 봐가며 조정)
- 셀 사이 gap은 8~10px 정도
- 최대 20회 기준 5열×4행이라 세로 길이가 과하게 길어지지 않으므로 **스크롤 처리는 불필요**. 다만 실제 렌더링 시 4행 높이가 카드 영역을 벗어나면 그때만 최소한의 `max-height` + `overflow-y: auto`를 안전장치로 추가 (필수 아님, 상황 보고 판단)

**3-5. 사각형 색상 농도**

- 색상은 기존보다 **훨씬 옅고 흐리게** 처리 (연한 오렌지 톤, 예: 기존 accent color에 낮은 opacity를 주거나 더 밝은 tint 색상 사용)
- 이유: 이 사각형들은 "아직 채우지 않은 잠재적 학습량"을 의미하므로, 진한 색이 아니라 여백처럼 은은하게 존재감만 있는 정도가 컨셉에 맞음
- **주의**: 실제 세션 진행 중 사각형이 진한 색으로 "채워지는" 진행률 표시 기능은 이번 작업 범위에 포함하지 않음. 이번엔 설정 화면 미리보기에서 전부 옅은 색으로만 표시하면 됨 (전부 동일한 옅은 색, 채워진/안 채워진 구분 없음)

**3-6. 요약 텍스트**

- 그리드 하단에 작은 요약 텍스트 한 줄 추가: "총 예상 시간: {최소학습시간} × {세트횟수} = {합계}" 형태 (예: "총 예상 시간: 5분 × 9세트 = 45분")
- 폰트 크기는 작게(caption 수준), 색상은 회색 계열로 은은하게 처리해 그리드보다 시각적 우선순위를 낮춤
- 하드코딩 금지 원칙에 따라 `strings/ko.ts`에 함수 템플릿으로 추가 (예: `setup.totalTimeSummary(minTime, setCount, total)`)

**3-7. 스타일 & 컴포넌트**

- 둥근 모서리는 기존 디자인 톤 유지, 색상만 옅게(3-5 참고)
- 컴포넌트 분리 권장: `SetCountGrid.tsx` 신규 컴포넌트로 만들어 `SetupScreen.tsx`에서 import (그리드 + 요약 텍스트를 함께 포함하는 형태 권장)

## 구현 시 참고

- 기존 "세트 반복 횟수" 값(state)을 그대로 슬라이더/그리드 렌더링에 사용 (기존 `useSettingsStore` 등 활용), 타입은 숫자로 단순화
- **무제한 옵션 삭제에 따른 정리**: `useSettingsStore`, `types/index.ts` 등에 있는 무제한 관련 상태(예: `isUnlimited` boolean)와 이를 참조하는 로직(세트 진행 시 무한 반복 처리 분기 등)을 찾아서 함께 제거. 세트 진행 로직이 무제한 분기에 의존하고 있었다면 최대 20회로 대체되는 흐름으로 자연스럽게 정리
- **범위 제외 사항**: 세션 진행 중 사각형이 진한 색으로 채워지는 진행률 표시 기능은 이번 작업에 포함하지 않음. 향후 별도 기능으로 설계 예정 (단순 색상 변화부터 시도 후, 필요 시 뱃지/반짝임 같은 연출 추가 검토)
- 텍스트 하드코딩 금지 컨벤션(CLAUDE.md/AGENTS.md) 준수
- 확률 슬라이더 등 다른 요소의 로직/스타일은 건드리지 않음

## 완료 후 확인 사항

- [ ] 라벨이 "1세트 최소 학습시간"으로 변경되었는지
- [ ] 항목 순서가 최소학습시간 → 세트반복횟수 → 휴식시간 → 확률 순인지
- [ ] 세트 반복 횟수가 숫자 입력창이 아닌 드래그 슬라이더(1~20회, 균등 간격)로 바뀌었는지, 무제한 체크박스가 삭제되었는지
- [ ] 드래그 중에는 숫자 라벨만 실시간으로 바뀌고, 손을 뗀 순간에만 그리드가 갱신되는지
- [ ] 그리드 갱신 시 사각형이 늘어나거나 줄어드는 애니메이션이 자연스럽게 적용되는지
- [ ] 그리드가 항상 5열 고정이면서 컨테이너 우측까지 여백 없이 꽉 차는지 (셀이 1fr 비율로 늘어나 직사각형이 되는지)
- [ ] 마지막 줄이 다 안 채워질 때 왼쪽부터 채워지고 오른쪽이 자연스럽게 비는지
- [ ] 사각형 안에 텍스트("5분" 등)가 들어가지 않고 순수 도형으로만 표시되는지
- [ ] 사각형 색상이 기존보다 훨씬 옅고 흐린 톤으로 바뀌었는지 (전부 동일한 옅은 색)
- [ ] 그리드 하단에 "총 예상 시간: 5분 × 9세트 = 45분" 형태의 작은 요약 텍스트가 표시되는지
- [ ] 기존 다른 기능(확률 로직, 몰입 모드 등)에 영향 없는지
