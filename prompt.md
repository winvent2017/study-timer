아래 스펙대로 "몰입 서프라이즈 타이머"라는 웹 앱의 MVP(0단계)를 만들어줘.

## 핵심 컨셉

숫자와 시간 압박 없이, 불확실성으로 몰입을 유도하는 학습/작업 타이머.

## 기술 스택

- Next.js (App Router) + TypeScript + Tailwind CSS
- 별도 백엔드/DB 없이 localStorage만 사용
- 단일 페이지 앱으로 시작

## 기능 명세

### 1. 세션 설정 화면

사용자가 시작 전에 아래 4가지를 설정:

- 최소 학습시간 (분 단위, 슬라이더나 입력, 기본값 5분)
- 휴식시간 (분 단위, 기본값 15분)
- 세트 반복 횟수 (숫자 입력, "무제한" 옵션 포함, 기본값 3)
- 알람이 안 울릴 확률 (0~100%, 기본값 40%)

설정 화면 하단에 안내 문구 표시:
"이 타이머는 가끔 예정된 시간에 울리지 않을 수 있어요. 당신도 몰랐던 집중 잠재력을 끌어올리기 위한 장치입니다."

### 2. 학습(작업) 타이머 화면

- 시작 버튼을 누르면 카운트다운 시작 (화면에 남은 시간 표시해도 됨, 최소시간 도달 전까지는)
- 최소시간 도달 전에는 "포기" 버튼 표시
  - 포기 버튼 클릭 시 격려/위로/자극 멘트 중 랜덤 1개를 모달로 보여주고 세션 종료 (예: "오늘은 여기까지! 시작한 것만으로도 충분해요", "다음엔 조금만 더 버텨볼까요?" 등 5개 이상 준비)
- 최소시간 도달 시:
  - 설정된 확률에 따라 랜덤 판정
  - **알람 울림**: "N분 다 됐어요! 더 할까요, 쉴까요?" 모달 표시. "더 하기" 선택 시 아래 '몰입 모드'로 전환. "쉬기" 선택 시 바로 종료 로직으로
  - **알람 안 울림**: 아무 알림 없이 자동으로 '몰입 모드'로 전환

### 3. 몰입 모드 (숫자 숨김)

- 남은/경과 시간을 전혀 표시하지 않음
- 대신 "열공 중" 느낌의 간단한 애니메이션(CSS 애니메이션이나 이모지 애니메이션으로 충분, 예: 책 아이콘이 움직이거나 진행바 없는 로딩 스피너 느낌)을 화면 중앙에 표시
- "그만하기" 버튼만 존재

### 4. 종료 연출

- "그만하기" 버튼 클릭 시:
  - 실제 총 경과시간과 원래 목표 최소시간을 비교해 배수 계산 (예: 목표 5분, 실제 52분 → 10.4배)
  - "5분만 하려 했는데 무려 52분! 목표의 10.4배를 해내셨어요 🎉" 형태의 축하 모달 + 간단한 confetti 애니메이션
  - "다음 세트로" / "휴식하기" 버튼 제공

### 5. 휴식 타이머

- 설정된 휴식시간만큼 카운트다운 (이 화면은 숫자 보여줘도 됨)
- 끝나면 다음 세트 시작 여부 확인

### 6. 세트 종료 요약

- 설정한 세트 수를 모두 마치면 오늘의 요약 화면 표시: 총 몰입시간, 세트별 목표 대비 배수, 최고 기록

## 디자인 톤

- 부담 없고 따뜻한 느낌 (압박감 있는 빨간색 카운트다운 지양)
- 모바일 반응형 필수 (가장 많이 쓰일 환경은 모바일 브라우저)

먼저 프로젝트 구조를 잡고, 1) 세션 설정 화면 → 2) 타이머+몰입모드 로직 → 3) 종료 연출 → 4) 휴식+요약 순서로 단계별로 구현해줘. 각 단계마다 어떤 파일을 만들었는지 알려줘.
