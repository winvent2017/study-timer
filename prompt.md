# 작업: pause 대화 프레임 디자인 수정

## 배경

직전 작업(pause → 대화 프레임 변형 연출)의 구현 결과, 프레임 좌표를 화면 비율(%)로 정의한 탓에 가로로 넓은 데스크톱 브라우저에서 프레임과 버튼이 화면 폭을 따라 옆으로 길게 늘어나는 문제가 발생했다. 시안의 의도는 세로 화면(360×640) 기준의 **세로로 길쭉한 프레임**이었다. 이번 작업은 로직 변경 없이 프레임의 크기·색상·타이포그래피만 수정한다.

## 수정 사항

### 1. 프레임 크기: 비율 → 고정 상한이 있는 세로형

- 프레임을 화면 비율 기반이 아닌 **고정 크기 기반**으로 변경:
  - 폭: `min(400px, 88vw)`
  - 높이: `min(560px, 78vh)`
- 화면(뷰포트)이 아무리 넓어도 프레임은 이 크기를 넘지 않는다. 세로가 가로보다 긴 형태가 기본.
- 가로 위치: 화면 중앙 정렬.
- 세로 위치: 프레임의 **하단 변이 pause 버튼의 y 위치를 지나가도록** 배치 (변형 연출의 연속성 유지 — 막대가 눕고 선이 그려질 때 하단 변에서 시작하므로). pause 버튼 위치가 화면 하단 중앙이므로 결과적으로 프레임은 중앙에서 약간 아래로 앵커된 형태가 된다. 이 배치로 프레임 상단이 화면을 벗어나는 경우가 생기면 높이 상한(78vh)을 줄여서 해결할 것.
- SVG path 좌표도 새 프레임 크기에 맞춰 재계산 (viewBox를 프레임 크기에 맞추면 계산이 단순해짐).

### 2. 테두리 색상: 흰색 → 웜 그레이

- 현재 테두리가 거의 순수한 흰색/웜 화이트로 렌더링되어 어두운 배경 위에서 너무 강하게 도드라진다.
- 선(테두리) 색을 **`#8a7f72`** (웜 그레이)로 변경. pause 막대와 동일한 색이어야 변형 연출의 연속성이 산다.
- 완성 상태의 테두리는 "은은하게 존재하는" 느낌이어야 한다. 필요 시 `#8a7f72`에 opacity 0.8~0.9를 더해 미세 조정 가능. 순수 흰색(#fff)이나 웜 화이트(#f2ead9)를 테두리에 쓰지 말 것.

### 3. 질문 텍스트 크기 확대

- "잠시 멈춰볼까요?" 텍스트: `clamp(20px, 5vw, 26px)`, font-weight 500 유지.
- 색상은 웜 화이트(#f2ead9 계열) 유지 — 텍스트는 밝아도 됨. 은은해야 하는 건 테두리뿐.

### 4. 버튼: 전체 폭 → 고정 폭 중앙 정렬

- 버튼 폭: `min(280px, 72%)` (프레임 폭 기준), 프레임 내 가로 중앙 정렬, 세로로 쌓임(간격 14px).
- padding: 상하 13px, border-radius: 10px 유지.
- 버튼 테두리: `rgba(242, 234, 217, 0.3)`, hover 시 배경 `rgba(242, 234, 217, 0.08)` (기존 스펙 유지).
- 버튼 텍스트 크기: 15px 내외.

## 변경하지 않는 것

- 변형 연출 타임라인(회전 220ms → 선 그리기 650ms → 콘텐츠 페이드인)과 easing은 그대로 유지
- strings/ko.ts의 pauseDialog 텍스트 그대로 유지 (텍스트 변경 없음)
- 버튼 핸들러, 경과 시간 일시정지/재개 로직, TODO 주석 그대로 유지
- prefers-reduced-motion 동작 그대로 유지

## 완료 후 확인 사항

- [ ] 넓은 데스크톱 창(1920px 등)에서 프레임이 세로로 길쭉한 형태(약 400×560)로 화면 중앙에 표시되는가
- [ ] 좁은 창/모바일 크기에서 프레임이 화면 안에 온전히 들어오는가 (테두리 잘림 없음)
- [ ] 테두리 색이 pause 막대와 동일한 웜 그레이(#8a7f72)로, 흰색으로 보이지 않는가
- [ ] 변형 연출(회전 → 선 그리기)이 새 프레임 크기에서도 자연스럽게 이어지는가 (막대 위치와 하단 변 시작점 일치)
- [ ] 버튼이 프레임 폭보다 좁은 고정 폭으로 중앙 정렬되어 있는가
- [ ] 질문 텍스트가 충분히 크고 위계(질문 > 버튼)가 명확한가
- [ ] 빌드/타입 에러 없음
